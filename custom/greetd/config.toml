# greetd configuration for the omarchy-bootc VM POC.
# Uses tuigreet to prompt for username/password and launch Hyprland.

[terminal]
vt = 1

[default_session]
command = "tuigreet --time --remember --cmd Hyprland"
user = "greeter"
